# Harmonization

PathwayMech harmonizes pathway and reaction identifiers from GO, MetaCyc, KEGG,
Rhea, ChEBI, UniProtKB, and EC records into a single pathway mechanism module.

Prefer stable external identifiers over local identifiers. Local identifiers
must be temporary and must be documented in `curation/decisions.tsv`.
